import { Utilities } from '../src/Utilities';

describe('testing Utility.ts', () => {
    // test('something should result in something', () => {
        
    //     expect(Utilities.timedAwait(() => returnTrueAfter(100), 200)).toHaveReturned();
    //     expect(Utilities.timedAwait(() => returnTrueAfter(300), 200)).toHaveBeenCalled();

    // });

    test('', () => {
        expect(true).toBe(true);
    });


});

// function returnTrueAfter(waitTime : number) 
// {
//     sleep(waitTime);
//     return true; 
// }

// function sleep(ms: number) {
//     return new Promise(resolve => setTimeout(resolve, ms));
// }

